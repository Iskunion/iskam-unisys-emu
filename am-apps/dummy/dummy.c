#include <am.h>
#include <klib-macros.h>

// char code[128];

int main(){
  // char *pc = code, ch;

  // while (ch = io_read(AM_UART_RX).data) *pc++ = ch;
  // *pc = '\0';
  // printf("%s\n", code);

  while(1);

  return 0;
}